# This is a libKCmdline definition document.

title = "grcan.txsize - definition.toml"
name = "grcan.txsize"
processor = "kernel"
description = '''Sets the size of the tx buffer.
Default: 1024'''

[syntax]
type = "<unsigned int> such that (txsize & ~0x1fffc0) == 0."
format = "<unsigned int> such that (txsize & ~0x1fffc0) == 0."
choices = []
allow_empty = true
