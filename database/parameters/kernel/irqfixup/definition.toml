# This is a libKCmdline definition document.

title = "irqfixup - definition.toml"
name = "irqfixup"
processor = "kernel"
description = '''When an interrupt is not handled search all handlers
for it. Intended to get systems with badly broken
firmware running.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
