# This is a libKCmdline definition document.

title = "xhci-hcd.quirks - definition.toml"
name = "xhci-hcd.quirks"
processor = "kernel"
description = '''A hex value specifying bitmask with supplemental xhci
host controller quirks. Meaning of each bit can be
consulted in header drivers/usb/host/xhci.h.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
