# This is a libKCmdline definition document.

title = "rcupdate.rcu_cpu_stall_suppress_at_boot - definition.toml"
name = "rcupdate.rcu_cpu_stall_suppress_at_boot"
processor = "kernel"
description = '''Suppress RCU CPU stall warning messages and
rcutorture writer stall warnings that occur
during early boot, that is, during the time
before the init task is spawned.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
