# This is a libKCmdline definition document.

title = "liteuart,<addr> - definition.toml"
name = "liteuart,<addr>"
processor = "kernel"
description = '''Start an early console on a litex serial port at the
specified address. The serial port must already be
setup and configured. Options are not yet supported.
'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
