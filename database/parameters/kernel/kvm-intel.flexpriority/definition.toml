# This is a libKCmdline definition document.

title = "kvm-intel.flexpriority - definition.toml"
name = "kvm-intel.flexpriority"
processor = "kernel"
description = '''[KVM,Intel] Control KVM's use of FlexPriority feature
(TPR shadow). Default is 1 (enabled). Disable by KVM if
hardware lacks support for it.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
