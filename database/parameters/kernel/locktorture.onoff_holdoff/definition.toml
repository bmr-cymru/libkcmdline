# This is a libKCmdline definition document.

title = "locktorture.onoff_holdoff - definition.toml"
name = "locktorture.onoff_holdoff"
processor = "kernel"
description = '''Set time (s) after boot for CPU-hotplug testing.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
