# This is a libKCmdline definition document.

title = "kvm-amd.nested - definition.toml"
name = "kvm-amd.nested"
processor = "kernel"
description = '''Control nested virtualization feature in
KVM/SVM. Default is 1 (enabled).
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
