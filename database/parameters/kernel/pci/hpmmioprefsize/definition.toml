# This is a libKCmdline definition document.

title = "hpmmioprefsize - definition.toml"
name = "hpmmioprefsize"
processor = "kernel"
description = '''The fixed amount of bus space which is
reserved for hotplug bridge's MMIO_PREF window.
Default size is 2 megabytes.'''

[syntax]
type = "nn[KMG]"
format = "nn[KMG]"
choices = []
allow_empty = true
