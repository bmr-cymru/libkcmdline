# This is a libKCmdline definition document.

title = "libata.noacpi - definition.toml"
name = "libata.noacpi"
processor = "kernel"
description = '''Disables use of ACPI in libata suspend/resume
when set.
'''

[syntax]
type = "<int>"
format = "<int>"
choices = []
allow_empty = true
