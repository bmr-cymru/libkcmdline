# This is a libKCmdline definition document.

title = "vmcp_cma - definition.toml"
name = "vmcp_cma"
processor = "kernel"
description = '''Sets the memory size reserved for contiguous memory
allocations for the vmcp device driver.
'''

[syntax]
type = "nn[MG]"
format = "nn[MG]"
choices = []
allow_empty = true
