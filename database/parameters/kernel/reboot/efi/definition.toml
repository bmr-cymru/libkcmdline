# This is a libKCmdline definition document.

title = "efi - definition.toml"
name = "efi"
processor = "kernel"
description = '''Use efi reset_system runtime service. If EFI is not
configured or the EFI reset does not work, the reboot
path attempts the reset using the keyboard controller.
'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
