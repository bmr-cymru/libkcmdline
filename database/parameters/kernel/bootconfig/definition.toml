# This is a libKCmdline definition document.

title = "bootconfig - definition.toml"
name = "bootconfig"
processor = "kernel"
description = '''Extended command line options can be added to an initrd
and this will cause the kernel to look for it.

See Documentation/admin-guide/bootconfig.rst
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
