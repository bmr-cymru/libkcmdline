# This is a libKCmdline definition document.

title = "cgroup_favordynmods - definition.toml"
name = "cgroup_favordynmods"
processor = "kernel"
description = '''Enable or Disable favordynmods.
Defaults to the value of CONFIG_CGROUP_FAVOR_DYNMODS.
'''

[syntax]
type = "enum"
format = "enum"
choices = ["true", "false"]
allow_empty = true
