# This is a libKCmdline definition document.

title = "conf1 - definition.toml"
name = "conf1"
processor = "kernel"
description = '''[X86] Force use of PCI Configuration Access
Mechanism 1 (config address in IO port 0xCF8,
data in IO port 0xCFC, both 32-bit).'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
