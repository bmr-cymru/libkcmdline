# This is a libKCmdline definition document.

title = "nosnp - definition.toml"
name = "nosnp"
processor = "kernel"
description = '''Do not enable SEV-SNP (applies to host/hypervisor
only). Setting 'nosnp' avoids the RMP check overhead
in memory accesses when users do not want to run
SEV-SNP guests.
'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
