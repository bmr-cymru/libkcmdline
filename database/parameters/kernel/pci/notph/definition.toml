# This is a libKCmdline definition document.

title = "notph - definition.toml"
name = "notph"
processor = "kernel"
description = '''[PCIE] If the PCIE_TPH kernel config parameter
is enabled, this kernel boot option can be used
to disable PCIe TLP Processing Hints support
system-wide.
'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
