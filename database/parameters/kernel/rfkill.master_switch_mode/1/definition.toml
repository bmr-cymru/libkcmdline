# This is a libKCmdline definition document.

title = "1 - definition.toml"
name = "1"
processor = "kernel"
description = '''The "airplane mode" button toggles between everything
blocked and the previous configuration.'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
