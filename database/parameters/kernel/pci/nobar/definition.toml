# This is a libKCmdline definition document.

title = "nobar - definition.toml"
name = "nobar"
processor = "kernel"
description = '''[X86] Do not assign address space to the
BARs that weren't assigned by the BIOS.'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
