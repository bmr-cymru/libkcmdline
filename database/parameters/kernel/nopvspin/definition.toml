# This is a libKCmdline definition document.

title = "nopvspin - definition.toml"
name = "nopvspin"
processor = "kernel"
description = '''Disables the qspinlock slow path using PV optimizations
which allow the hypervisor to 'idle' the guest on lock
contention.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
