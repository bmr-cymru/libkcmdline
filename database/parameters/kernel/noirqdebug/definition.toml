# This is a libKCmdline definition document.

title = "noirqdebug - definition.toml"
name = "noirqdebug"
processor = "kernel"
description = '''Disables the code which attempts to detect and
disable unhandled interrupt sources.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
