# This is a libKCmdline definition document.

title = "torture.disable_onoff_at_boot - definition.toml"
name = "torture.disable_onoff_at_boot"
processor = "kernel"
description = '''Prevent the CPU-hotplug component of torturing
until after init has spawned.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
