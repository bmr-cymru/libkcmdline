# This is a libKCmdline definition document.

title = "irqpoll - definition.toml"
name = "irqpoll"
processor = "kernel"
description = '''When an interrupt is not handled search all handlers
for it. Also check all handlers each timer
interrupt. Intended to get systems with badly broken
firmware running.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
