# This is a libKCmdline definition document.

title = "pnpbios - definition.toml"
name = "pnpbios"
processor = "kernel"
description = ''''''

[syntax]
type = "enum"
format = "enum"
choices = ["on", "off", "curr", "res", "no-curr", "no-res"]
allow_empty = true
