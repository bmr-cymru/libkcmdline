# This is a libKCmdline definition document.

title = "random.trust_bootloader - definition.toml"
name = "random.trust_bootloader"
processor = "kernel"
description = '''[KNL,EARLY] Disable trusting the use of the a seed
passed by the bootloader (if available) to
initialize the kernel's RNG.
'''

[syntax]
type = "off"
format = "off"
choices = []
allow_empty = true
