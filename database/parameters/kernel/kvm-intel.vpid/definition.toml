# This is a libKCmdline definition document.

title = "kvm-intel.vpid - definition.toml"
name = "kvm-intel.vpid"
processor = "kernel"
description = '''Control KVM's use of Virtual Processor
Identification feature (tagged TLBs). Default is 1
(enabled). Disable by KVM if hardware lacks support
for it.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
