# This is a libKCmdline definition document.

title = "scftorture.weight_all_wait - definition.toml"
name = "scftorture.weight_all_wait"
processor = "kernel"
description = '''The probability weighting to use for the
smp_call_function_all() function with a
non-zero "wait" parameter.  See weight_single
and weight_many.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
