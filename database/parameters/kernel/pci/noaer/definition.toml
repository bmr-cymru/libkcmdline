# This is a libKCmdline definition document.

title = "noaer - definition.toml"
name = "noaer"
processor = "kernel"
description = '''[PCIE] If the PCIEAER kernel config parameter is
enabled, this kernel boot option can be used to
disable the use of PCIE advanced error reporting.'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
