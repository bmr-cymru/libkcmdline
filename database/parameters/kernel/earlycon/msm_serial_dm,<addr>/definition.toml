# This is a libKCmdline definition document.

title = "msm_serial_dm,<addr> - definition.toml"
name = "msm_serial_dm,<addr>"
processor = "kernel"
description = '''Start an early, polled-mode console on an msm serial
dm port at the specified address. The serial port
must already be setup and configured. Options are not
yet supported.
'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
