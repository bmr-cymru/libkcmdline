# This is a libKCmdline definition document.

title = "protect_image - definition.toml"
name = "protect_image"
processor = "kernel"
description = '''Turn on image protection during restoration
(that will set all pages holding image data
during restoration read-only).
'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
