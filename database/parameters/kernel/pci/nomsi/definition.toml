# This is a libKCmdline definition document.

title = "nomsi - definition.toml"
name = "nomsi"
processor = "kernel"
description = '''[MSI] If the PCI_MSI kernel config parameter is
enabled, this kernel boot option can be used to
disable the use of MSI interrupts system-wide.'''

[syntax]
type = ""
format = ""
choices = []
allow_empty = true
