# This is a libKCmdline definition document.

title = "init_pkru - definition.toml"
name = "init_pkru"
processor = "kernel"
description = '''Specify the default memory protection keys rights
register contents for all processes.  0x55555554 by
default (disallow access to all but pkey 0).  Can
override in debugfs after boot.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
