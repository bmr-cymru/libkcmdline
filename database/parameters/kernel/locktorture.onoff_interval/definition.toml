# This is a libKCmdline definition document.

title = "locktorture.onoff_interval - definition.toml"
name = "locktorture.onoff_interval"
processor = "kernel"
description = '''Set time (s) between CPU-hotplug operations, or
zero to disable CPU-hotplug testing.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
