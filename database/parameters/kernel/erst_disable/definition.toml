# This is a libKCmdline definition document.

title = "erst_disable - definition.toml"
name = "erst_disable"
processor = "kernel"
description = '''Disable Error Record Serialization Table (ERST)
support.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
