# This is a libKCmdline definition document.

title = "pata_legacy.qdi - definition.toml"
name = "pata_legacy.qdi"
processor = "kernel"
description = '''Set to non-zero to probe QDI controllers.  By default
set to 1 if CONFIG_PATA_QDI_MODULE, 0 otherwise.
'''

[syntax]
type = "<int>"
format = "<int>"
choices = []
allow_empty = true
