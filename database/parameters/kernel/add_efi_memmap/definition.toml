# This is a libKCmdline definition document.

title = "add_efi_memmap - definition.toml"
name = "add_efi_memmap"
processor = "kernel"
description = '''Include EFI memory map in
kernel's map of available physical RAM.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
