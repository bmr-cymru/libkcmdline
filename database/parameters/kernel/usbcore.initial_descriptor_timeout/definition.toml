# This is a libKCmdline definition document.

title = "usbcore.initial_descriptor_timeout - definition.toml"
name = "usbcore.initial_descriptor_timeout"
processor = "kernel"
description = '''[USB] Specifies timeout for the initial 64-byte
USB_REQ_GET_DESCRIPTOR request in milliseconds
(default 5000 = 5.0 seconds).
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
