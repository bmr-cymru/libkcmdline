# This is a libKCmdline definition document.

title = "locktorture.bind_writers - definition.toml"
name = "locktorture.bind_writers"
processor = "kernel"
description = '''Specify the list of CPUs to which the writers are
to be bound.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
