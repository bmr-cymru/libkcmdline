# This is a libKCmdline definition document.

title = "torture.ftrace_dump_at_shutdown - definition.toml"
name = "torture.ftrace_dump_at_shutdown"
processor = "kernel"
description = '''even if there were no errors.  This can be a
very costly operation when many torture tests
with rotating-rust storage.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
