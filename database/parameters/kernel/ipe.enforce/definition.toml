# This is a libKCmdline definition document.

title = "ipe.enforce - definition.toml"
name = "ipe.enforce"
processor = "kernel"
description = '''Determine whether IPE starts in permissive (0) or
enforce (1) mode. The default is enforce.
'''

[syntax]
type = "<bool>"
format = "<bool>"
choices = []
allow_empty = true
