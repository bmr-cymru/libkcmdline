# This is a libKCmdline definition document.

title = "stress_slb - definition.toml"
name = "stress_slb"
processor = "kernel"
description = '''them frequently to increase the rate of SLB faults
on kernel addresses.
'''

[syntax]
type = "flag"
format = "flag"
choices = []
allow_empty = true
